# Embedding Service (placeholder)

This will be a small FastAPI service wrapping Sentence-Transformers
(`all-MiniLM-L6-v2`) that exposes `POST /embed`.

Not implemented yet — this is built in Step 5 of the roadmap.
